//all time highscore, if(din score > alltime) {endre på det, jeg har noen vage tanker} else if(skjekk de andre i synkende rekkefølge)
const nameInput = document.getElementById("nameInput")
function registrerName(){
    let playerName = nameInput.value
}


let pHighScore = localStorage.getItem("pHighScore") || 0

if(pHighScore > playerArr[0].highScore) {
    //noe
}

else if (pHighScore > playerArr[1].highScore) {
    //noe
}